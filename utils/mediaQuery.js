const sizes = {
  xs: '640px',
  sm: '768px',
  md: '1024px',
  lg: '1280px',
};

const mediaQuery = {
  xs: `(max-width: ${sizes.xs})`,
  sm: `(max-width: ${sizes.sm})`,
  md: `(max-width: ${sizes.md})`,
  lg: `(max-width: ${sizes.lgå})`,
};

export default mediaQuery;
